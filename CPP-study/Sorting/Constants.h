#ifndef __CONSTANTS__
#define __CONSTANTS__

#define DEFAULT_ARRAY_SIZE 1024		 // the default size of the array
#define ISNEG(X) (!((X) > 0) && ((X) != 0)) // macro to ccheck the signedness of the value in the variable 



#endif //for #ifndef _CONSTANTS__